<block wx:if="{{$taroCompReady}}">
    <scroll-view scroll-y="{{true}}" scroll-with-animation="{{true}}">
        <view class="index">
            <item-view wx:for="{{loopArray12}}" wx:for-item="index" wx:for-index="_anonIdx2" compid="{{index.$compid__12}}"></item-view>
        </view>
    </scroll-view>
</block>